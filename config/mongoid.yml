
development:
  clients:
    default:
      uri: <%=Rails.application.credentials[Rails.env.to_sym][:MONGODB]%>
      options:
        server_selection_timeout: 5
production:
  clients:
    default:
      uri: <%=Rails.application.credentials[Rails.env.to_sym][:MONGODB]%>
      options:
        server_selection_timeout: 5
      